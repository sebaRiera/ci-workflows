# Dummies solo para `next build` en CI.
# Coolify inyecta las variables reales en deploy — no uses secretos acá.

NEXT_TELEMETRY_DISABLED=1
NEXT_PUBLIC_API_URL=https://example.invalid
NEXT_PUBLIC_CDN_BASE_URL=https://example.invalid/cdn
NEXT_PUBLIC_BASE_URL=https://example.invalid
NEXT_PUBLIC_TIMEZONE=America/Argentina/Buenos_Aires
API_URL=https://example.invalid
NEXTAUTH_URL=https://example.invalid
NEXTAUTH_SECRET=change-me-ci-only
PAY_API_BASE_URL=https://example.invalid
PAY_API_CLIENT_ID=change-me-client-id
PAY_API_CLIENT_SECRET=change-me-client-secret
